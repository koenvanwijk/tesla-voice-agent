# Tesla Voice Agent

Tesla-first voice assistant with a GitHub Pages frontend and a fully local AI backend on your own laptop.

No cloud AI API is required. Speech-to-text runs with `faster-whisper`, the LLM runs through local `Ollama`, and speech output uses the browser's built-in speech synthesis.

See the setup instructions below once the initial project files are committed.
